import React from "react";
import { Window } from "./window";
import { Box, Text } from "@chakra-ui/core";

export const Browser = ({ url }) => {
  return <Window title="browser" content={<></>} bg="#fff" />;
};
